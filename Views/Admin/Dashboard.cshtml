@{
    ViewData["Title"] = "Admin Dashboard";
}

<h2 class="mb-4">Admin Dashboard</h2>

<div class="row row-cols-1 row-cols-md-3 g-4">
    <div class="col">
        <div class="card h-100 shadow-sm">
            <div class="card-body text-center">
                <h5 class="card-title">👥 Users</h5>
                <p class="card-text">View and manage registered users.</p>
                <a href="@Url.Action("Index", "UserManagement")" class="btn btn-outline-primary">View All Users</a>
            </div>
        </div>
    </div>

    <div class="col">
        <div class="card h-100 shadow-sm">
            <div class="card-body text-center">
                <h5 class="card-title">📦 Products</h5>
                <p class="card-text">Add, edit, or remove products from inventory.</p>
                <a href="@Url.Action("List", "Products")" class="btn btn-outline-success">Manage Products</a>
            </div>
        </div>
    </div>

    <div class="col">
        <div class="card h-100 shadow-sm">
            <div class="card-body text-center">
                <h5 class="card-title">📑 Orders</h5>
                <p class="card-text">Review customer orders and summaries.</p>
                <a href="@Url.Action("Index", "Orders")" class="btn btn-outline-secondary">View Order Summaries</a>
            </div>
        </div>
    </div>
</div>
